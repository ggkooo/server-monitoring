services:
  edge-nginx:
    image: nginx:1.27-alpine
    container_name: server-monitoring-edge-nginx
    depends_on:
      - frontend
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - server_monitoring_network
